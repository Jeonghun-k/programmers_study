# 코드 알고리즘
1. 제공된 리스트의 길이를 저장할 변수를 만든다.
2. 리스트의 길이만큼 새로운 배열을 생성한다.<br>
    배열생성은 int[] 배열명 = new int[길이];
    로 만들어야 된다
3. 반복문을 이용하여 i를 0부터 제공된 배열 길이만큼 증가시킨다.
4. 새로 만들어진 배열의 i번째 인덱스에 기존 배열의 (길이-1-i)번째 인덱스 값을 저장시킨다
5. 이걸 반복한다
