# 문자 리스트를 문자열로 변경

## 코드
```python
def solution(arr):
    answer = ''.join(arr)
    return answer
```

파이썬에서 `join`함수는
매개변수로 들어온 리스트에 있는 요소 하나하나를 합쳐서 하나의 문자열로 바꾸어 반환하는 함수입니다.

그래서 제공된 리스트를 바로 `answer`에 `join`함수로 추가를 하게되면 리스트를 문자열로 변경할 수있다.
